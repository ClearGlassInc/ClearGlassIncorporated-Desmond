name: Deploy ClearGlass Agent Live

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Deployment target"
        required: true
        default: "fly"
        type: choice
        options:
          - fly
      dry_run:
        description: "Validate deployment path without pushing to host"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
  push:
    branches:
      - main
    paths:
      - 'services/clearglass_agent_service/**'
      - 'fly.toml'
      - '.github/workflows/deploy-clearglass-agent-live.yml'

permissions:
  contents: read
  deployments: write

concurrency:
  group: clearglass-agent-live-${{ github.ref }}
  cancel-in-progress: false

jobs:
  preflight:
    name: Preflight service and deployment route
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      dry_run: ${{ steps.route.outputs.dry_run }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set deployment route
        id: route
        shell: bash
        run: |
          set -euo pipefail
          TARGET="${{ github.event.inputs.target || 'fly' }}"
          DRY_RUN="${{ github.event.inputs.dry_run || 'false' }}"

          if [[ "$TARGET" != "fly" ]]; then
            echo "Unsupported target: $TARGET"
            exit 1
          fi

          echo "dry_run=$DRY_RUN" >> "$GITHUB_OUTPUT"
          {
            echo "## Deployment route"
            echo "- Target: $TARGET"
            echo "- Dry run: $DRY_RUN"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Validate expected files
        shell: bash
        run: |
          set -euo pipefail
          test -f fly.toml
          test -f services/clearglass_agent_service/Dockerfile
          test -f services/clearglass_agent_service/requirements.txt
          test -f services/clearglass_agent_service/main.py
          echo "Required deployment files exist."

      - name: Validate Python application import
        env:
          CLEARGLASS_AGENT_API_KEYS: preflight-test-key
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -r services/clearglass_agent_service/requirements.txt
          python - <<'PY'
          from services.clearglass_agent_service.main import app
          print(f"Validated FastAPI app: {app.title}")
          PY

      - name: Validate Docker image builds
        shell: bash
        run: |
          set -euo pipefail
          docker build \
            -f services/clearglass_agent_service/Dockerfile \
            -t clearglass-agent-service:preflight \
            .

  deploy_fly:
    name: Deploy to Fly.io
    runs-on: ubuntu-latest
    needs: preflight
    timeout-minutes: 30
    environment:
      name: production-fly
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Validate required Fly secrets
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          CLEARGLASS_AGENT_API_KEYS: ${{ secrets.CLEARGLASS_AGENT_API_KEYS }}
        shell: bash
        run: |
          set -euo pipefail

          if [[ -z "${FLY_API_TOKEN:-}" ]]; then
            echo "Missing GitHub secret: FLY_API_TOKEN" | tee -a "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          if [[ -z "${CLEARGLASS_AGENT_API_KEYS:-}" ]]; then
            echo "Missing GitHub secret: CLEARGLASS_AGENT_API_KEYS" | tee -a "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          echo "Required Fly secrets are present." >> "$GITHUB_STEP_SUMMARY"

      - name: Authenticate Fly token
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          flyctl version
          flyctl auth whoami
          echo "Fly token authenticated." >> "$GITHUB_STEP_SUMMARY"

      - name: Dry-run Fly deployment
        if: needs.preflight.outputs.dry_run == 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          flyctl status --app clearglass-agent-service || true
          docker build \
            -f services/clearglass_agent_service/Dockerfile \
            -t clearglass-agent-service:dry-run \
            .
          echo "Dry-run complete. No live Fly deployment executed." >> "$GITHUB_STEP_SUMMARY"

      - name: Ensure Fly app exists
        if: needs.preflight.outputs.dry_run != 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          if flyctl status --app clearglass-agent-service >/dev/null 2>&1; then
            echo "Fly app exists."
          else
            echo "Creating Fly app: clearglass-agent-service"
            flyctl apps create clearglass-agent-service
          fi

      - name: Set Fly runtime secrets
        if: needs.preflight.outputs.dry_run != 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          CLEARGLASS_AGENT_API_KEYS: ${{ secrets.CLEARGLASS_AGENT_API_KEYS }}
        shell: bash
        run: |
          set -euo pipefail
          flyctl secrets set \
            CLEARGLASS_AGENT_API_KEYS="$CLEARGLASS_AGENT_API_KEYS" \
            CLEARGLASS_ALLOWED_ORGS="ClearGlassInc" \
            CLEARGLASS_ALLOWED_ORIGINS="https://clearglassinc.github.io" \
            CLEARGLASS_AGENT_VERSION="0.1.0" \
            --app clearglass-agent-service

      - name: Deploy Fly service
        if: needs.preflight.outputs.dry_run != 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          flyctl deploy \
            --config fly.toml \
            --remote-only \
            --app clearglass-agent-service

      - name: Smoke test Fly health endpoint
        if: needs.preflight.outputs.dry_run != 'true'
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --retry 18 --retry-delay 5 --retry-all-errors \
            https://clearglass-agent-service.fly.dev/health

      - name: Dump Fly diagnostics on failure
        if: failure()
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        shell: bash
        run: |
          set +e
          echo "## Fly diagnostics" >> "$GITHUB_STEP_SUMMARY"
          flyctl status --app clearglass-agent-service
          flyctl releases --app clearglass-agent-service
          flyctl logs --app clearglass-agent-service --no-tail
