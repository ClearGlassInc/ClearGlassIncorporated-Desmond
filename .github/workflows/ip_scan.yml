name: IP Scan

on:
  workflow_dispatch:

jobs:
  ip_scan:
    runs-on: ubuntu-latest
    steps:
      - name: IP scan wrapper disabled
        run: echo "IP scan wrapper disabled until reusable workflow is repaired."
