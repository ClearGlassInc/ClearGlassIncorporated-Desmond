name: OSIT CI

on:
  pull_request:
    paths:
      - "osit/**"
      - ".github/workflows/osit-ci.yml"
  push:
    branches:
      - main
    paths:
      - "osit/**"
      - ".github/workflows/osit-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: osit-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: osit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: osit/pyproject.toml
      - name: Install
        run: python -m pip install -e '.[dev]'
      - name: Dependency integrity
        run: python -m pip check
      - name: Compile
        run: python -m compileall -q src tests
      - name: Ruff
        run: ruff check src tests --output-format=github
      - name: Unit and export tests
        run: pytest --cov=osit --cov-report=term-missing
