name: Pages

on:
  workflow_dispatch:

jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
      - name: Pages wrapper disabled
        run: echo "Pages wrapper disabled until reusable deployment workflow is repaired."
