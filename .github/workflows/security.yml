name: Security

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
  schedule:
    - cron: "0 6 * * 1"   # Monday 06:00 UTC — weekly scan

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency Review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      # Full enforcement: a real vulnerable-dependency finding on any PR
      # (including human PRs touching runtime manifests like
      # deployment/artemis/app/requirements.txt) must block the merge.
      # This requires Dependency Graph to be enabled under
      # Settings → Code security and analysis; if it is ever disabled the
      # action errors with "Dependency review is not supported on this
      # repository", which is the correct, visible signal to re-enable it.
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0

  secret-scan:
    name: Secret Pattern Scan
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
      - name: Detect hardcoded secrets
        run: |
          python3 - <<'PYEOF'
          import re, sys
          from pathlib import Path

          PATTERNS = {
            "AWS access key":        r"AKIA[0-9A-Z]{16}",
            "GitHub PAT (ghp_)":     r"ghp_[A-Za-z0-9]{36}",
            "GitHub PAT (ghs_)":     r"ghs_[A-Za-z0-9]{36}",
            "GitLab PAT":            r"glpat-[A-Za-z0-9_-]{20}",
            "Generic API key":       r"(?i)api[_-]?key\s*[:=]\s*['\"][A-Za-z0-9/+]{20,}['\"]",
            "Private key header":    r"-----BEGIN (RSA|EC|DSA|OPENSSH) PRIVATE KEY-----",
          }

          EXTENSIONS = {
            ".py", ".js", ".mjs", ".cjs", ".ts", ".tsx",
            ".env", ".json", ".yaml", ".yml", ".sh",
          }

          findings = []
          root = Path(".")
          for path in root.rglob("*"):
            if not path.is_file():
              continue
            if path.suffix not in EXTENSIONS:
              continue
            rel = str(path)
            if ".git/" in rel or "node_modules/" in rel:
              continue
            try:
              text = path.read_text(encoding="utf-8", errors="ignore")
            except OSError:
              continue
            for label, pattern in PATTERNS.items():
              if re.search(pattern, text):
                findings.append(f"{rel}: {label}")

          if findings:
            for f in findings:
              print(f"::error::{f}")
            sys.exit(1)
          print("No hardcoded secrets detected.")
          PYEOF

  workflow-lint:
    name: Workflow YAML Lint
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install pyyaml
      - name: Validate workflow YAML
        run: |
          python3 - <<'PYEOF'
          import sys, yaml
          from pathlib import Path

          errors = []
          for wf in sorted(Path(".github/workflows").glob("*.y*ml")):
            try:
              data = yaml.safe_load(wf.read_text(encoding="utf-8"))
              if not isinstance(data, dict):
                errors.append(f"{wf}: root is not a mapping")
            except yaml.YAMLError as exc:
              errors.append(f"{wf}: {exc}")

          if errors:
            for e in errors:
              print(f"::error::{e}")
            sys.exit(1)
          print(f"All {len(list(Path('.github/workflows').glob('*.y*ml')))} workflow files are valid YAML.")
          PYEOF
