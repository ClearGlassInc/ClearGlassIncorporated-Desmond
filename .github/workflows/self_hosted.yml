name: Self Hosted

on:
  workflow_dispatch:

jobs:
  self_hosted:
    runs-on: ubuntu-latest
    steps:
      - name: Self hosted wrapper disabled
        run: echo "Self hosted wrapper disabled until reusable deployment workflow is repaired."
