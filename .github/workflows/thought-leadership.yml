name: 4-D Corporate / Thought Leadership

# Reusable domain workflow for the Corporate dimension of the 4-D Dominance
# pipeline. Runs the orchestrator in dry-run mode and prints the JSON report.

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    name: Corporate domain dry-run
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.11"
      - name: Run 4-D corporate pipeline (dry-run)
        run: python -m four_d_dominance.pipeline --domain corporate --json
