# ClearGlass Side Store — storefront environment (copy to .env.local; never commit secrets)
# Stripe secret key — server-side only. Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_replace_me
# Public base URL used to build success/cancel redirect URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3001
