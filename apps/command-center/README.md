# ClearGlass Burlington Growth Command Centre

Static, GitHub-Pages-compatible command-centre prototype for the growth engine. It intentionally displays draft/zero states unless verified operational data is connected. Do not wire external publication, ad spend, outreach, personal-data collection, or CRM mutations without the approval controls in `config/approval-policies.yaml` and `artemis.growth_engine`.
