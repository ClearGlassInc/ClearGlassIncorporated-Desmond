suppression_required: true
rules:
  - suppress explicit opt-outs immediately
  - suppress bounced addresses
  - suppress recipients after configured final follow-up
  - suppress government officials unless approval exists
  - never commit personal suppression records to git
