# ClearGlassInc Artemis — Production Frontend Prompt

Enhance the existing ClearGlassInc Artemis homepage without deleting, replacing, reordering, or obscuring any existing content, section, action, spacing relationship, accessibility label, or navigation path. Build an additive, defense-grade command layer with restrained cyan and violet accents over the current light editorial system.

## Interface primitives

- Add orbital node icons, concentric status rings, signal markers, micro-reticles, kinetic corner brackets, precision grid lines, data halos, and compact control glyphs.
- Add soft-glass primary capsules, luminous outline actions, telemetry toggles, pill controls, contextual command chips, and state metadata badges.
- Frame existing cards and headings with instrumentation; never place decoration over readable copy.

## Visual system

Use `#07111f` for command ink, `#00b8d9` for verified telemetry, `#7357ff` for intelligence depth, and green only for confirmed active state. Use one-pixel precision strokes, low-opacity grids, layered glass blur, controlled radial light, and generous negative space. Preserve the current typography, palette, content hierarchy, and premium editorial character.

## Motion and interaction

Animate telemetry with synchronized 2–24 second cycles: slow orbital rotation, faint vertical scan drift, periodic node breathing, and a quiet active-state pulse. Pointer-aware cards may reveal a bounded radial energy field. Hover raises interactive controls by no more than two pixels; focus must show a high-contrast cyan ring. Provide a telemetry on/off control, and disable nonessential animation under `prefers-reduced-motion`.

## Placement

Place the precision grid and reticles behind the hero, command chips immediately after existing hero actions, metadata at the edge of the control cluster, and corner brackets inside existing capability, product, command, operations, and value cards. Keep mobile ornaments smaller and hide secondary metadata where space is constrained.

## Implementation constraints

Implement the layer as isolated CSS and dependency-free progressive-enhancement JavaScript. Keep all controls keyboard accessible and expose toggle state through `aria-pressed`. Use passive pointer listeners, avoid layout-thrashing scroll handlers, preserve GitHub Pages compatibility, and make the experience fully usable when JavaScript or motion is unavailable.
