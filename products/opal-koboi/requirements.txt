# Facial Recognition System - Python Dependencies
# Install with: pip install -r requirements.txt

# Core facial recognition
face_recognition>=1.3.0
dlib>=19.24.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Security hardening — encryption-at-rest for the biometric template store
# (opal_security.EncryptedVault). Required as of v2.0.
cryptography>=42.0.0

# Optional: GPU acceleration (if you have NVIDIA GPU)
# opencv-python-headless>=4.8.0  # Use instead of opencv-python for servers

# For development/testing
# pytest>=7.4.0
# black>=23.0.0

# System requirements:
# - Python 3.8 or higher
# - CMake (for dlib compilation)
# - C++ compiler
#   Windows: Visual Studio Build Tools
#   Linux: gcc/g++
#   Mac: Xcode Command Line Tools

# Installation notes:
# If face_recognition installation fails:
# 1. Install cmake: pip install cmake
# 2. Install dlib: pip install dlib
# 3. Then: pip install face_recognition
#
# On Windows, if errors persist:
# - Install Visual Studio Build Tools from:
#   https://visualstudio.microsoft.com/downloads/
# - Select "Desktop development with C++"
#
# On Mac:
# - Install Xcode: xcode-select --install
# - Install Homebrew cmake: brew install cmake
#
# On Linux (Ubuntu/Debian):
# - sudo apt-get install cmake
# - sudo apt-get install python3-dev
# - sudo apt-get install libopenblas-dev liblapack-dev
