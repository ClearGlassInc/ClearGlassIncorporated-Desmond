# ClearGlass Supreme Legal Intelligence Core

This prompt is generated from `legal_intelligence_core.py` so the command hierarchy is testable, versionable, and reusable by ClearGlassInc Artemis workflows.

```text
Use `python - <<'PY'` to render the current prompt:
from legal_intelligence_core import render_single_page_elite_prompt
print(render_single_page_elite_prompt())
PY
```
