# Test tooling
pytest==9.0.3
pytest-cov==7.1.0

# Type stubs used during static analysis (optional, no runtime impact)
# Add runtime dependencies here as the bot layer grows
jsonschema>=4.21,<5  # control-surface feed contract validation
